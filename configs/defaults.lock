# defaults.lock — plugins embedded into a release build by
# scripts/build-binaries.sh (requires `-tags embed`; see internal/bootstrap).
#
# Reviewable, diffable list: one plugin per line, name and version
# separated by whitespace. Lines starting with # and blank lines are
# ignored.
#
# name  version
tacho   2.4.0
bell    1.1.0
